{
  "devDependencies": {
    "jest-environment-jsdom": "^29.7.0",
    "vite": "^6.3.5"
  },
  "scripts": {
    "dev": "vite",
    "test": "npx jest --testTimeout=30000 --colors --verbose --runInBand --config jest.config.js app.test.js"
  }
}
